import { KeyboardSettingsWorkspace } from "@/components/radio-settings/keyboard-settings/keyboard-settings-workspace"

export default function KeyboardSettingsPage() {
  return <KeyboardSettingsWorkspace />
}
